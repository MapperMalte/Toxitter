package Avatar.Elemental.fire.AI.classic.artifacts;

import Avatar.Elemental.fire.AI.classic.Backpropagation;

public class BackpropagationFactory {
    public Backpropagation MAKE()
    {
        return new Backpropagation();
    }
}
